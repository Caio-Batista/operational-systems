Cada algoritmo foi dado um conjunto de frames N vezes e com isso foi 
plotado no gráfico a quantidade de pages fault para cada um deles.
Portanto, esperávamos que o algoritmo aging fosse melhor, pois teoricamente é o que é implementado no kernel do linux, porém teve casos que o second-chance foi melhor.
